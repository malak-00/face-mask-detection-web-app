# CPU-only build of PyTorch — smaller download (~200 MB vs ~2 GB for CUDA)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu
torchvision==0.21.0+cpu

fastapi==0.110.0
uvicorn[standard]==0.29.0
anyio>=3.7.1,<5.0.0
python-multipart==0.0.9
Pillow>=10.3.0
numpy>=1.26.0
requests==2.31.0
# headless build — no Qt/GUI, avoids DLL issues on Windows
opencv-python-headless==4.10.0.84
scikit-learn>=1.3.0